% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/wrappers-core.R
\name{sorter2_stage1b}
\alias{sorter2_stage1b}
\title{Run the SORTER2 Stage1B step}
\usage{
sorter2_stage1b(
  input_dir,
  output_dir,
  ref,
  loci,
  clust2id = 0.7,
  recluster = FALSE,
  contignum = 20,
  contiglen = 300,
  aliter = 1000,
  indelrep = 0.1,
  idformat = "onlysample",
  verbose = 1L,
  python = Sys.getenv("SORTER2R_PYTHON", "python"),
  conda_env = Sys.getenv("SORTER2R_CONDA_ENV", ""),
  conda = Sys.getenv("SORTER2R_CONDA", "conda"),
  script_dir = NULL,
  overwrite = FALSE,
  dry_run = FALSE
)
}
\arguments{
\item{input_dir}{Directory containing the Stage1A output
(\verb{*_assembly/} subdirs with SPAdes contigs).}

\item{output_dir}{Directory where \verb{diploids/} and \verb{diploidclusters/} will
be created.}

\item{ref}{Reference file path.}

\item{loci}{Number of loci to process.}

\item{clust2id}{Clustering threshold.}

\item{recluster}{Logical flag for reclustering. When \code{TRUE}, reuses
\verb{assembly_workfiles/} (assembled contigs) from a previous Stage1B run
in \code{output_dir} instead of re-assembling.}

\item{contignum}{Contig count threshold.}

\item{contiglen}{Contig length threshold.}

\item{aliter}{Alignment iterations.}

\item{indelrep}{Indel representation threshold.}

\item{idformat}{Identifier format.}

\item{verbose}{Integer verbosity level. \code{1L} (default) shows Python-level
section headers; \code{0L} is fully silent; \code{2L} echoes the command, enables
per-item debug output (\code{-v}), and shows subprocess tool output.}

\item{python}{Python executable to use.}

\item{conda_env}{Optional conda environment name.}

\item{conda}{Conda executable to use when \code{conda_env} is set.}

\item{script_dir}{Directory containing the vendored SORTER2 scripts.}

\item{overwrite}{If \code{TRUE}, remove existing output before running. When
\code{recluster = TRUE}, only prior Stage1B output is removed and
\verb{assembly_workfiles/} is preserved so \code{-reclust} can reuse it.
Otherwise \code{output_dir} is deleted entirely.}

\item{dry_run}{If \code{TRUE}, return the command without executing it.}
}
\value{
Path to \code{output_dir} (for use with \code{tar_file()}).
}
\description{
Run the SORTER2 Stage1B step
}
