% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/infer.R
\name{sorter2_infer_hybrids}
\alias{sorter2_infer_hybrids}
\title{Identify candidate hybrid or polyploid samples from Stage 1B output}
\usage{
sorter2_infer_hybrids(input_dir, min_mean = 2)
}
\arguments{
\item{input_dir}{Directory containing Stage 1B output. Must contain a
\verb{diploids/} subdirectory with a file matching
\verb{ALLsamples_consensusallele_*_SUMMARY_TABLE.csv}.}

\item{min_mean}{Minimum mean consensus alleles per locus required to flag
a sample as a hybrid/polyploid candidate. Default \code{2.0}: samples with on
average more than two allele copies per locus are flagged.}
}
\value{
A data frame with one row per sample, sorted by descending
\code{mean_per_locus}, with columns:
\describe{
\item{sample}{Sample name (trailing \verb{_} stripped; matches the names
accepted by the \code{hybrid_samples} argument of \code{\link[=sorter2_stage3]{sorter2_stage3()}}).}
\item{total_alleles}{Sum of consensus alleles across all loci.}
\item{n_loci}{Number of loci with at least one consensus allele.}
\item{mean_per_locus}{\code{total_alleles / n_loci}. Primary ploidy proxy:
diploids typically show 1–2; values above 2 suggest polyploidy.}
\item{candidate}{\code{TRUE} if \code{mean_per_locus >= min_mean}.}
}
}
\description{
Reads the consensus-allele summary table produced by \code{\link[=sorter2_stage1b]{sorter2_stage1b()}}
and computes per-sample allele-count statistics useful for distinguishing
diploids from hybrids or polyploids. Polyploids carry extra genome copies,
so their total consensus-allele count across loci is proportionally
elevated relative to diploids.
}
\details{
Interpretation guidance (from SORTER2 documentation, full-dataset scale):
\itemize{
\item Diploids: ~1–2 consensus alleles per locus on average
\item Tetraploids: ~2–3 per locus
\item Hexaploids: ~4+ per locus
}

Note that absolute counts depend on the number of loci analysed; use
\code{mean_per_locus} for cross-dataset comparisons rather than \code{total_alleles}.

For a complete hybrid assessment, combine these allele-count statistics
with ADMIXTURE ancestry proportions and phylogenetic placement.
}
