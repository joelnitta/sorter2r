% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/wrappers-core.R
\name{sorter2_stage2}
\alias{sorter2_stage2}
\title{Run the SORTER2 Stage2 step}
\usage{
sorter2_stage2(
  input_assemblies,
  input_clusters,
  output_dir,
  reads_dir = NULL,
  phasequal = 20,
  aliter = 1000,
  indelrep = 0.1,
  idformat = "phase",
  threads = 1L,
  verbose = 1L,
  python = Sys.getenv("SORTER2R_PYTHON", "python"),
  conda_env = Sys.getenv("SORTER2R_CONDA_ENV", ""),
  conda = Sys.getenv("SORTER2R_CONDA", "conda"),
  script_dir = NULL,
  overwrite = FALSE,
  dry_run = FALSE
)
}
\arguments{
\item{input_assemblies}{Directory containing the Stage1A output
(\verb{*_assembly/} subdirs with SPAdes contigs).}

\item{input_clusters}{Directory containing the Stage1B output
(\verb{diploidclusters/} and \verb{diploids/} subdirs).}

\item{output_dir}{Directory where \verb{diploids_phased/} will be created.}

\item{reads_dir}{Optional directory containing raw FASTQ files
(\verb{*_R1.fastq} / \verb{*_R2.fastq}). Required when Stage 1A was run with
\code{trim = FALSE}. When \code{NULL} (the default), Stage2 expects Trim Galore
output (\verb{*_R1_val_1.fq}) inside each \verb{*_assembly/} subdirectory.}

\item{phasequal}{Phase quality threshold.}

\item{aliter}{Alignment iterations.}

\item{indelrep}{Indel representation threshold.}

\item{idformat}{Identifier format.}

\item{threads}{Number of parallel worker processes for per-sample phasing
and per-locus alignment. Default \code{1L} (serial).}

\item{verbose}{Integer verbosity level. \code{1L} (default) shows Python-level
section headers; \code{0L} is fully silent; \code{2L} echoes the command, enables
per-item debug output (\code{-v}), and shows subprocess tool output.}

\item{python}{Python executable to use.}

\item{conda_env}{Optional conda environment name.}

\item{conda}{Conda executable to use when \code{conda_env} is set.}

\item{script_dir}{Directory containing the vendored SORTER2 scripts.}

\item{overwrite}{If \code{TRUE}, delete \code{output_dir} before running.}

\item{dry_run}{If \code{TRUE}, return the command without executing it.}
}
\value{
Path to \code{output_dir} (for use with \code{tar_file()}).
}
\description{
Run the SORTER2 Stage2 step
}
