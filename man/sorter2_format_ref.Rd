% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/aaa-utils.R
\name{sorter2_format_ref}
\alias{sorter2_format_ref}
\title{Reformat a reference FASTA for SORTER2}
\usage{
sorter2_format_ref(input, output = NULL)
}
\arguments{
\item{input}{Path to the input reference FASTA file.}

\item{output}{Path for the reformatted output file. Defaults to the input
path with \verb{_sorter2} appended before the extension.}
}
\value{
Path to the output file.
}
\description{
Rewrites sequence IDs to the \verb{>L\{n\}_\{Family-Genus-species\}} format
required by Stage 1B. The locus number \code{n} is taken from the \verb{L\{n\}}
substring after the last \code{-} in the original ID; sequences whose
post-hyphen tag does not already carry an \verb{L\{n\}_} prefix are assigned
sequential numbers starting after the highest embedded one. The
taxonomic label (fields 2–4 of the original \verb{_}-delimited name) is
appended with \code{-} as a separator, ensuring every output ID is unique.
}
