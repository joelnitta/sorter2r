% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/filter.R
\name{sorter2_filter_assemblies}
\alias{sorter2_filter_assemblies}
\title{Copy ingroup assembly folders from Stage 1A output, excluding outgroups}
\usage{
sorter2_filter_assemblies(
  input_dir,
  output_dir,
  exclude_samples = character(),
  overwrite = FALSE
)
}
\arguments{
\item{input_dir}{Directory containing Stage 1A output (the \verb{*_assembly}
subdirectories produced by \code{\link[=sorter2_stage1a]{sorter2_stage1a()}}).}

\item{output_dir}{Directory to create containing only ingroup assemblies.
Must not already exist unless \code{overwrite = TRUE}.}

\item{exclude_samples}{Character vector of sample names to exclude
(outgroups). Names should match the \verb{\{sample\}_assembly} folder prefix —
for example, \code{"Wade3565_cintermedium"} excludes
\verb{Wade3565_cintermedium_assembly/}.}

\item{overwrite}{If \code{TRUE}, delete \code{output_dir} before running.}
}
\value{
Path to \code{output_dir} (for use with \code{tar_file()}).
}
\description{
SORTER2 Stages 1B onward should be run on ingroup samples only; outgroup
assemblies inflate allele counts and ADMIXTURE complexity. This function
creates a new directory containing only the ingroup \verb{*_assembly} folders
from a Stage 1A output directory, leaving the original untouched.
}
