% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/wrappers-core.R
\name{sorter2_progenitor_processor}
\alias{sorter2_progenitor_processor}
\title{Process progenitor sequences from Stage 3 output}
\usage{
sorter2_progenitor_processor(
  input_dir,
  output_dir,
  mapfile,
  outgroups,
  minseq = 1L,
  filterundiff = FALSE,
  python = Sys.getenv("SORTER2R_PYTHON", "python"),
  conda_env = Sys.getenv("SORTER2R_CONDA_ENV", ""),
  conda = Sys.getenv("SORTER2R_CONDA", "conda"),
  script_dir = NULL,
  dry_run = FALSE,
  verbose = 1L
)
}
\arguments{
\item{input_dir}{Directory containing Stage 3 FASTA output (read-only).}

\item{output_dir}{Directory to write processed output into. Created if
it does not exist.}

\item{mapfile}{Path to a CSV file with three columns:
\code{hybrid}, \code{progenitor}, \code{clade}.}

\item{outgroups}{Path to a plain-text file listing outgroup IDs to
remove (one per line).}

\item{minseq}{Minimum number of phased sequence pairs required per
progenitor set across all loci. The Python script doubles this
value internally before applying the filter. Default \code{1L}.}

\item{filterundiff}{If \code{TRUE}, discard loci where a hybrid
sample has fewer than two distinct progenitor clades represented.
Default \code{FALSE}.}

\item{python}{Python executable to use.}

\item{conda_env}{Optional conda environment name.}

\item{conda}{Conda executable to use when \code{conda_env} is set.}

\item{script_dir}{Directory containing the vendored SORTER2 scripts.}

\item{dry_run}{If \code{TRUE}, return the command without executing it.}

\item{verbose}{Integer verbosity level. \code{1L} (default) shows
Python-level section headers; \code{0L} is fully silent; \code{2L}
echoes the command and shows subprocess tool output.}
}
\value{
Path to \code{output_dir} (for use with \code{tar_file()}).
}
\description{
Runs \code{SORTER2_ProgenitorProcessor.py}. Collapses progenitor IDs
to user-defined clades, removes outgroup sequences, filters by minimum
sequence count, removes duplicate IDs, and optionally retains only loci
with at least two differentiated progenitor clades.
}
\details{
The Stage 3 \code{input_dir} is never modified, so its targets hash
remains stable.
}
