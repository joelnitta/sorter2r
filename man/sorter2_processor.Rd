% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/wrappers-core.R
\name{sorter2_processor}
\alias{sorter2_processor}
\title{Run the SORTER2 Processor step}
\usage{
sorter2_processor(
  input_dir,
  output_dir = input_dir,
  repfilt = 0.5,
  majorclusters = 2,
  keepal = TRUE,
  stage2 = FALSE,
  stage3 = FALSE,
  dovcf = TRUE,
  reads_dir = NULL,
  success_codes = 0L,
  overwrite = FALSE,
  python = Sys.getenv("SORTER2R_PYTHON", "python"),
  conda_env = Sys.getenv("SORTER2R_CONDA_ENV", ""),
  conda = Sys.getenv("SORTER2R_CONDA", "conda"),
  script_dir = NULL,
  dry_run = FALSE,
  verbose = 1L
)
}
\arguments{
\item{input_dir}{Directory containing the SORTER2 pipeline outputs
(\verb{diploids/}, \verb{diploidclusters/}, and optionally \verb{diploids_phased/} and
\verb{phaseset/}).}

\item{output_dir}{Directory where processed results will be written.}

\item{repfilt}{Repeat filter threshold.}

\item{majorclusters}{Number of major clusters.}

\item{keepal}{Logical flag for keeping alignments.}

\item{stage2}{Logical flag for including Stage2 output.}

\item{stage3}{Logical flag for including Stage3 output.}

\item{dovcf}{Logical flag for writing VCF output.}

\item{reads_dir}{Optional directory containing raw FASTQ files
(\verb{*_R1.fastq} / \verb{*_R2.fastq}). Required when Stage 1A was run
with \code{trim = FALSE}. When \code{NULL} (the default), the Processor
expects Trim Galore output (\verb{*_R1_val_1.fq}) inside each
\verb{*_assembly/} subdirectory.}

\item{success_codes}{Integer vector of exit codes treated as success.
Default \code{0L}. Pass \code{c(0L, 1L)} if outputs are known to be correct
and the script is exiting 1 on a re-run (e.g. interrupted pipeline).}

\item{overwrite}{If \code{TRUE}, delete \code{output_dir} before running.}

\item{python}{Python executable to use.}

\item{conda_env}{Optional conda environment name.}

\item{conda}{Conda executable to use when \code{conda_env} is set.}

\item{script_dir}{Directory containing the vendored SORTER2 scripts.}

\item{dry_run}{If \code{TRUE}, return the command without executing it.}

\item{verbose}{Integer verbosity level. \code{1L} (default) shows Python-level
section headers; \code{0L} is fully silent; \code{2L} echoes the command, enables
per-item debug output (\code{-v}), and shows subprocess tool output.}
}
\value{
Path to \code{output_dir} (for use with \code{tar_file()}).
}
\description{
Run the SORTER2 Processor step
}
