% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/wrappers-core.R
\name{sorter2_stage1a}
\alias{sorter2_stage1a}
\title{Run the SORTER2 Stage1A step}
\usage{
sorter2_stage1a(
  reads_dir,
  output_dir,
  spades = TRUE,
  trim = TRUE,
  clean_spades_tmp = FALSE,
  python = Sys.getenv("SORTER2R_PYTHON", "python"),
  conda_env = Sys.getenv("SORTER2R_CONDA_ENV", ""),
  conda = Sys.getenv("SORTER2R_CONDA", "conda"),
  script_dir = NULL,
  overwrite = FALSE,
  dry_run = FALSE,
  verbose = 1L
)
}
\arguments{
\item{reads_dir}{Directory containing the input FASTQ files.}

\item{output_dir}{Directory where the assembled output will be created.}

\item{spades}{Logical flag for SPAdes assembly.}

\item{trim}{Logical flag for TrimGalore trimming.}

\item{clean_spades_tmp}{If \code{TRUE}, delete SPAdes intermediate K-mer graph
directories (\verb{K21/}, \verb{K33/}, etc.) after each sample assembles. Saves
substantial disk space at the cost of preventing SPAdes restarts.
Default \code{FALSE}.}

\item{python}{Python executable to use.}

\item{conda_env}{Optional conda environment name.}

\item{conda}{Conda executable to use when \code{conda_env} is set.}

\item{script_dir}{Directory containing the vendored SORTER2 scripts.}

\item{overwrite}{If \code{TRUE}, delete \code{output_dir} before running.}

\item{dry_run}{If \code{TRUE}, return the command without executing it.}

\item{verbose}{Integer verbosity level. \code{1L} (default) shows Python-level
section headers; \code{0L} is fully silent; \code{2L} echoes the command, enables
per-item debug output (\code{-v}), and shows subprocess tool output.}
}
\value{
Path to \code{output_dir} (for use with \code{tar_file()}).
}
\description{
Run the SORTER2 Stage1A step
}
