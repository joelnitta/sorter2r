% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/wrappers-core.R
\name{sorter2_haplominer}
\alias{sorter2_haplominer}
\title{Run the SORTER2 Hapl-O-Miner step}
\usage{
sorter2_haplominer(
  input_dir,
  output_dir,
  organellar_ref,
  coverage = 50,
  depth = 5,
  reads_dir = NULL,
  threads = 1L,
  clean_workfiles = FALSE,
  python = Sys.getenv("SORTER2R_PYTHON", "python"),
  conda_env = Sys.getenv("SORTER2R_CONDA_ENV", ""),
  conda = Sys.getenv("SORTER2R_CONDA", "conda"),
  script_dir = NULL,
  dry_run = FALSE,
  verbose = 1L
)
}
\arguments{
\item{input_dir}{Directory containing Stage 1A \verb{*_assembly/}
subdirectories.}

\item{output_dir}{Directory where all Hapl-O-Miner output will be
written.}

\item{organellar_ref}{Path to the haploid organellar reference FASTA
(e.g. a chloroplast genome).}

\item{coverage}{Minimum reference coverage percentage threshold.
Samples below this value are excluded from the final alignment.}

\item{depth}{Minimum mean read-depth threshold.  Samples below this
value are excluded from the final alignment.}

\item{reads_dir}{Optional directory containing raw FASTQ files
(\verb{*_R1.fastq} / \verb{*_R2.fastq}). Required when Stage 1A was run with
\code{trim = FALSE}. When \code{NULL} (the default), Hapl-O-Miner expects
Trim Galore output (\verb{*_R1_val_1.fq}) inside each \verb{*_assembly/}
subdirectory.}

\item{threads}{Number of parallel worker processes for per-sample
mapping. Default \code{1L} (serial).}

\item{clean_workfiles}{If \code{TRUE}, delete the intermediate workfiles
directory (BAMs, raw consensus FASTAs, per-sample stat files) after
the run completes. Saves substantial disk space; the compiled
\code{readstats_cp.csv} and final consensus FASTAs are unaffected.
Default \code{FALSE}.}

\item{python}{Python executable to use.}

\item{conda_env}{Optional conda environment name.}

\item{conda}{Conda executable to use when \code{conda_env} is set.}

\item{script_dir}{Directory containing the vendored SORTER2 scripts.}

\item{dry_run}{If \code{TRUE}, return the command without executing it.}

\item{verbose}{Integer verbosity level. \code{1L} (default) shows
Python-level section headers; \code{0L} is fully silent; \code{2L} echoes
the command and enables per-item debug output (\code{-v}).}
}
\value{
Path to \code{output_dir} (for use with \code{tar_file()}).
}
\description{
Mines haploid organellar genomes (e.g. chloroplast) from off-target
by-catch reads in a SORTER2 Stage 1A dataset.  Reads from each
\verb{*_assembly/} subdirectory are mapped to a user-supplied haploid
organellar reference; per-sample consensus sequences are generated
and filtered by minimum coverage and read-depth thresholds.
}
